<mvc:View controllerName="todoapp.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true" async="true"
    xmlns="sap.m">
    <Shell id="shell">
        <App id="app">
            <pages>
                <Page id="page" title="{i18n>title}">
                    <content>
                        <List items="{/TODOSet}" headerText="TODO List" growing="true" growingThreshold="4" growingScrollToLoad="false">
                            <items>
                                <StandardListItem type="Active" title="{Title}" description="{Description}" press=".onItemSelected" icon="{
                                            parts :[
                                                { path : 'Status' }
                                            ],
                                            formatter: '.formatter.getStatus'
                                        }" iconDensityAware="false" iconInset="false" />
                            </items>

                            <headerToolbar>
                                <OverflowToolbar>
                                    <Title text="TODO List" level="H2" />
                                    <ToolbarSpacer />
                                    <Button text="Add new Todo" icon="sap-icon://add" press=".onNewTODO" />
                                </OverflowToolbar>
                            </headerToolbar>

                        </List>
                    </content>
                </Page>
            </pages>
        </App>
    </Shell>
</mvc:View>
